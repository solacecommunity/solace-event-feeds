{
  "rules": {
    "TICKER": {
      "schema": {
        "type": "string"
      },
      "rule": {
        "name": "TICKER",
        "type": "string",
        "group": "StringRules",
        "rule": "enum",
        "enum": [
          "GOOG"
        ]
      }
    }
  },
  "publishSettings": {
    "count": "10",
    "interval": "1",
    "delay": "0"
  }
}